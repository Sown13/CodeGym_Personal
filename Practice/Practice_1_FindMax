Begin
    Input N = Số lượng giá trị trong dãy số
    Input N giá trị liên tiếp a0,a1,...an
    set Max = a0
    set i = 1
    While   i<=n
          If  ai > Max
                set Max = ai
          End If
          set i = i+1
    End While
End